import React from 'react'
import { Layout } from 'antd'
const { Header } = Layout

function Landing() {
    return (
        <Layout>
            <Header style={{color: 'white'}}>
                Welcome to....
            </Header>
        </Layout>
    )
}

export default Landing
